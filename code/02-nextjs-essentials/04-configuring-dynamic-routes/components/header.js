export default function Header() {
  return (
    <>
    // we create a new component, pasting in these two lines only from page.
    // We separate component folder from App folder, leaving it for routing and pages.
    
      <img src="/logo.png" alt="A server surrounded by magic sparkles." />
      <h1>Welcome to this NextJS Course!</h1>
    </>
  );
}
